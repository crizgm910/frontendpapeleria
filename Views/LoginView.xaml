<UserControl x:Class="PapeleriaDB.Views.LoginView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:iconPacks="http://metro.mahapps.com/winfx/xaml/iconpacks"
             mc:Ignorable="d"
             d:DesignHeight="700" d:DesignWidth="1100"
             FontFamily="Segoe UI, Inter, Roboto, Arial, sans-serif">
    

    <Grid>
        <Grid.Background>
            <RadialGradientBrush Center="0.5,0.5" GradientOrigin="0.5,0.5" RadiusX="0.7" RadiusY="0.7">
                <GradientStop Color="#F5F3FF" Offset="0"/>
                <GradientStop Color="#EEF2F6" Offset="1"/>
            </RadialGradientBrush>
        </Grid.Background>

        <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center" Width="440">
            <Border Width="64" Height="64" Background="#8B5CF6" CornerRadius="16" Margin="0,0,0,16" HorizontalAlignment="Center">
                <iconPacks:PackIconMaterial Kind="BookOpenPageVariant" Width="32" Height="32" Foreground="White" HorizontalAlignment="Center" VerticalAlignment="Center"/>
            </Border>

            <TextBlock Text="Papeler&#237;a DB" FontSize="28" FontWeight="Bold" Foreground="#1E1B4B" HorizontalAlignment="Center" Margin="0,0,0,4"/>
            <TextBlock Text="Sistema de Control - Punto de Venta" FontSize="14" Foreground="#64748B" HorizontalAlignment="Center" Margin="0,0,0,32"/>

            <Border Background="White" CornerRadius="16" Padding="40" BorderBrush="#F1F5F9" BorderThickness="1">
                <Border.Effect>
                    <DropShadowEffect Color="#0F172A" BlurRadius="30" ShadowDepth="4" Opacity="0.05"/>
                </Border.Effect>

                <StackPanel>
                    <TextBlock Text="Iniciar sesi&#243;n" FontSize="20" FontWeight="Bold" Foreground="#0F172A" Margin="0,0,0,4"/>
                    <TextBlock Text="Ingresa tus credenciales autorizadas" FontSize="13" Foreground="#64748B" Margin="0,0,0,24"/>

                    <TextBlock Text="USUARIO O CORREO ELECTR&#211;NICO" FontSize="11" FontWeight="Bold" Foreground="#475569" Margin="0,0,0,8"/>
                    <TextBox Tag="ej. admin@papeleria.com"
                             Text="{Binding Usuario, UpdateSourceTrigger=PropertyChanged}" 
                             KeyDown="Input_KeyDown"
                             Margin="0,0,0,20"/>

                    <TextBlock Text="CONTRASE&#209;A" FontSize="11" FontWeight="Bold" Foreground="#475569" Margin="0,0,0,8"/>
                    <Grid Margin="0,0,0,12">
                        <PasswordBox x:Name="PasswordInput" KeyDown="Input_KeyDown"/>
                        <!-- Lock Icon -->
                        <iconPacks:PackIconMaterial Kind="LockOutline" Width="18" Height="18" Foreground="#94A3B8" HorizontalAlignment="Right" VerticalAlignment="Center" Margin="0,0,14,0"/>
                    </Grid>
                    
                    <TextBlock Text="{Binding ErrorMessage}" Foreground="#EF4444" FontSize="13" FontWeight="SemiBold" TextWrapping="Wrap" Margin="0,0,0,12">
                        <TextBlock.Style>
                            <Style TargetType="TextBlock">
                                <Setter Property="Visibility" Value="Visible"/>
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding ErrorMessage}" Value="">
                                        <Setter Property="Visibility" Value="Collapsed"/>
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </TextBlock.Style>
                    </TextBlock>

                    <Grid Margin="0,0,0,24">
                        <CheckBox Content="Recordar dispositivo" IsChecked="{Binding RecordarDispositivo, Mode=TwoWay}" Foreground="#475569" VerticalAlignment="Center" HorizontalAlignment="Left"/>
                        <TextBlock Text="&#191;Olvidaste tu contrase&#241;a?" Foreground="#8B5CF6" FontWeight="SemiBold" Cursor="Hand" VerticalAlignment="Center" HorizontalAlignment="Right"/>
                    </Grid>

                    <Button Command="{Binding IniciarSesionCommand}" CommandParameter="{Binding ElementName=PasswordInput}" Height="48" BorderThickness="0" Cursor="Hand" IsDefault="True">
                        <Button.Template>
                            <ControlTemplate TargetType="Button">
                                <Border x:Name="BtnBorder" Background="#8B5CF6" CornerRadius="8">
                                    <Grid>
                                        <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" VerticalAlignment="Center" x:Name="NormalContent">
                                            <iconPacks:PackIconMaterial Kind="LoginVariant" Width="18" Height="18" Foreground="White" Margin="0,0,8,0" VerticalAlignment="Center"/>
                                            <TextBlock Text="Iniciar Sesi&#243;n" Foreground="White" FontSize="15" FontWeight="SemiBold" VerticalAlignment="Center"/>
                                        </StackPanel>
                                        <TextBlock x:Name="LoaderText" Text="Validando..." Foreground="White" FontSize="15" FontWeight="SemiBold" HorizontalAlignment="Center" VerticalAlignment="Center" Visibility="Collapsed"/>
                                    </Grid>
                                </Border>
                                <ControlTemplate.Triggers>
                                    <Trigger Property="IsMouseOver" Value="True">
                                        <Setter TargetName="BtnBorder" Property="Background" Value="#7C3AED"/>
                                    </Trigger>
                                    <Trigger Property="IsEnabled" Value="False">
                                        <Setter TargetName="BtnBorder" Property="Background" Value="#C084FC"/>
                                    </Trigger>
                                    <DataTrigger Binding="{Binding IsLoading}" Value="True">
                                        <Setter TargetName="NormalContent" Property="Visibility" Value="Collapsed"/>
                                        <Setter TargetName="LoaderText" Property="Visibility" Value="Visible"/>
                                    </DataTrigger>
                                </ControlTemplate.Triggers>
                            </ControlTemplate>
                        </Button.Template>
                    </Button>
                </StackPanel>
            </Border>
            
            <TextBlock Text="Acceso restringido solo a personal autorizado." FontSize="11" Foreground="#94A3B8" HorizontalAlignment="Center" Margin="0,24,0,0"/>
            <TextBlock Text="Papeler&#237;a DB v2.0 - Sistema interno de gesti&#243;n" FontSize="11" Foreground="#CBD5E1" HorizontalAlignment="Center" Margin="0,8,0,0"/>
        </StackPanel>
    </Grid>
</UserControl>
